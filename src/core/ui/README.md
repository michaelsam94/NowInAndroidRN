# Shared UI (Phase 7)

Reusable components and theme tokens (`NewsResourceCard`, `BookmarkNoteDialog`, MD3 `ThemeProvider`).

**Rules:** May import `@core/domain` types for props; no feature-specific logic.
