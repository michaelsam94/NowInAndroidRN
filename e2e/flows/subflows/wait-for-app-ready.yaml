appId: com.nowinandroidrn.demo
---
# E2E APK sets extra.e2e — navigation mounts while bootstrap finishes in background.
- extendedWaitUntil:
    visible:
      id: nia:app-ready
    timeout: 120000
- extendedWaitUntil:
    visible:
      id: foryou:screen
    timeout: 120000
