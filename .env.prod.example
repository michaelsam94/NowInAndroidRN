# Production flavor — set your backend URL (Retrofit BACKEND_URL equivalent)
EXPO_PUBLIC_FLAVOR=prod
EXPO_PUBLIC_API_BASE=https://your-nia-api.example.com

# EAS cloud builds (also set as GitHub Actions secret EAS_PROJECT_ID for CI)
# EAS_PROJECT_ID=
